# Environment Variables Example
# Copy this file to .env.local and fill in your actual values

# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# =============================================================================
# CONVEX DEPLOYMENT
# =============================================================================
# Your Convex deployment URL (get this from Convex Dashboard)
CONVEX_DEPLOYMENT=dev:.... # team: your-team, project: your-project
NEXT_PUBLIC_CONVEX_URL=https://your-deployment-name.convex.cloud

# =============================================================================
# CONVEX AUTH (Google OAuth)
# =============================================================================
# IMPORTANT: These must be set in Convex environment, NOT just .env.local!
# Use Convex CLI to set these:
#   npx convex env set AUTH_GOOGLE_ID your_google_client_id
#   npx convex env set AUTH_GOOGLE_SECRET your_google_client_secret
# 
# Get Google OAuth credentials from: https://console.cloud.google.com/
# 1. Create OAuth 2.0 Client ID 
# 2. Set authorized redirect URI to: https://your-deployment.convex.site/api/auth/callback/google
#
# These are just for reference - actual values go in Convex environment:
# AUTH_GOOGLE_ID=1234567890-abcdefghijklmnop.apps.googleusercontent.com
# AUTH_GOOGLE_SECRET=GOCSPX-your_actual_secret_here

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================
# Site URL for redirects and metadata (optional)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
SITE_URL=http://localhost:3000

# Logging level: trace, debug, info, warn, error, silent (optional)
NEXT_PUBLIC_LOG_LEVEL=info

# Skip environment validation - useful for Docker builds (optional)
# SKIP_ENV_VALIDATION=true
